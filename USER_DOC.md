This project provides a WordPress website hosted on a Debian VM using Docker containers.
The infrastructure includes:

- **NGINX web server** (HTTPS only, port 443)
- **WordPress site** with PHP-FPM
- **MariaDB database**
